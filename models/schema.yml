version: 2

sources:
  - name: raw
    schema: gz_raw_data
    description:  greenweez raw data
    tables:
      - name: sales
        identifier: raw_gz_sales
        description: raw sportswear sales
      - name: product
        identifier: raw_gz_product
        description: lists of products
      - name: ship
        identifier: raw_gz_ship
        description:  shipment table
